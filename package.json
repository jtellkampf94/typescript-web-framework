{
  "devDependencies": {
    "typescript": "^4.2.4"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "concurrently": "^6.0.1"
  },
  "scripts": {
    "start:db": "json-server -w db.json",
    "start:parce": "parcel index.html",
    "start": "concurrently npm:start:*"
  }
}
